So I think after little bit of exploration it's really making sense.. so step 0 is to route your terminal and essentially launch LazyVim. Step 1 is to use the shortcut `<leader>ff>` to launch the files, it's really need and I also want to say that's the telescope maybe...? actually right another big idea is to use `<leader>/` to open the grep. Then if you're using the tool for ideation launch `<leader.>` to launch the scratchpad. Actually write out every logical thing that you're doing and that's literally a GOATED behavior.

So another big thing to know that's weird is that they have something called like `yanking`... but essentially yanking is the way how you copy some shit. use it in conjunction with `visual mode` and you're gucci. of course don't forget `p` to paste.

Grep is essentially the matching system, packaged... (i still have a surface level of understanding, excuse my amateurism.

Question: how do you see the command history? is it useful?

Question: how to move files and create folders? I also remember the videos saying something about trees? 
